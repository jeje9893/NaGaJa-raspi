# 부저 / 버튼 하드웨어 테스트 (라즈베리파이)

실기기에서 **부저(GPIO18)** 와 **해제 버튼(GPIO17)** 이 실제로 동작하는지
화면의 테스트 버튼만으로 확인하는 도구입니다. **Firebase·serviceAccountKey 불필요** —
순수 하드웨어 점검용이라 라즈베리파이에 코드만 올리면 바로 실행됩니다.

> 부저/버튼 코드는 `nagaja_bridge.py` 의 것을 그대로 재사용합니다.
> 즉 이 테스트가 통과하면 실제 브리지의 알람도 동일하게 동작합니다.

---

## 구성

| 파일 | 역할 |
|---|---|
| `hardware_test.py` | GPIO 제어 + WebSocket(양방향) 서버 |
| `hardware_test.html` | 7인치 터치 화면용 테스트 UI (800×480) |

기존 운영 파일(`nagaja_bridge.py`, `timer_ui.html`)은 **건드리지 않습니다.**

---

## 배선 (BCM 핀 번호)

```
부저(+)  ── GPIO18 (pin 12)        버튼 한쪽 ── GPIO17 (pin 11)
부저(-)  ── GND   (pin 6, 14 등)   버튼 반대 ── GND
```
- 버튼은 내부 풀업(PUD_UP)을 사용하므로 **GND로 누르는** 방식입니다(별도 저항 불필요).
- 능동 부저(active buzzer) 기준입니다.

---

## 실행

```bash
# 1) 서버 실행 (라즈베리파이)
cd ~/nagaja/ui            # hardware_test.py 가 있는 위치
source ~/nagaja/venv/bin/activate
pip install websockets gpiozero lgpio   # 최초 1회 (라즈베리파이 5 호환)
python3 hardware_test.py

# 2) 화면 띄우기 (다른 터미널 / 키오스크)
chromium --kiosk hardware_test.html
#   또는 그냥 브라우저로 hardware_test.html 열기
```

서버 로그에 `[실기기(GPIO)] 하드웨어 테스트 서버 시작` 이 보이면 GPIO가 활성화된 것입니다.
(`gpiozero`/`lgpio` 가 없는 PC에서는 `[시뮬레이션]` 모드로 떠서 UI·통신만 점검됩니다.)

---

## 화면 버튼 설명

| 버튼 | 동작 | 확인 포인트 |
|---|---|---|
| 🔔 **기상 알람 시작** | 0.5초 간격으로 부저 울림(최대 120초) | 실제 기상 알람과 동일. 좌측 표시등이 깜빡임 |
| ⏹ **알람 정지** | 울리는 알람 해제 | **물리 버튼과 똑같은 동작** |
| 🔊 **삐삐 (2회)** | '삐 삐' 두 번 | 상태 전환음(`double_beep`) 테스트 |
| 📢 **누르는 동안 ON** | 누르고 있는 동안만 부저 ON | 부저 단독 점검(꾹 눌렀다 떼기) |

### 좌측 상태판
- **부저 표시등**: 부저가 울리는 동안 빨갛게 켜짐 → 소리와 화면이 일치하는지 확인.
- **물리 버튼(GPIO17)**: 실제 버튼을 누르면 초록색으로 번쩍이며 시각을 표시 → **버튼 배선/입력 확인**.
- **연결 / GPIO 상태 점**: 서버 연결과 실기기/시뮬레이션 모드 표시.
- **로그**: 모든 이벤트가 시각과 함께 기록됨.

---

## 권장 점검 순서

1. **부저 단독** — "📢 누르는 동안 ON" 을 꾹 → 소리 + 표시등 ON 확인, 떼면 OFF.
2. **삐삐** — "🔊 삐삐" → 짧게 두 번 울리는지.
3. **기상 알람 → 물리 버튼 해제** — "🔔 기상 알람 시작" 으로 울린 뒤
   **실제 물리 버튼**을 눌러 → 소리가 멈추고 좌측 '물리 버튼'이 초록으로 번쩍이는지.
   (버튼이 안 눌리면 배선/핀 번호 확인)
4. **화면 정지** — 다시 알람을 켜고 "⏹ 알람 정지" 로도 멈추는지.

---

## 문제 해결

| 증상 | 점검 |
|---|---|
| 화면에 "시뮬레이션 모드" | GPIO 라이브러리 미설치 또는 PC에서 실행 중 → 라즈베리파이에서 `pip install gpiozero lgpio` |
| `Cannot determine SOC peripheral base address` | 라즈베리파이 5에서 구형 `RPi.GPIO` 사용 시 발생. 본 코드는 `gpiozero`+`lgpio` 를 쓰므로 `pip install gpiozero lgpio` 후 재실행 |
| 부저 표시등은 켜지는데 소리 없음 | 부저 배선/극성, 능동 부저 여부, GPIO18 연결 확인 |
| 물리 버튼을 눌러도 화면 반응 없음 | 버튼이 GPIO17↔GND 인지, 눌림 방향, 접촉 불량 확인 |
| "서버에 연결되어 있지 않습니다" | `hardware_test.py` 실행 여부, 같은 기기에서 여는지(ws://localhost:8765) |
| 포트 충돌 | `nagaja_bridge.py` 가 떠 있으면 8765 충돌 → 둘 중 하나만 실행 |
