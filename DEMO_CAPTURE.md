# 구동화면 촬영 가이드 (라즈베리파이 + 카메라)

라즈베리파이 7인치 화면에 **나가자 타이머 UI**를 상태별로 띄워 **카메라로 촬영**하기 위한
데모 환경입니다. 수정하지 않은 `timer_ui.html` 을 실제 WebSocket 파이프라인으로 구동하므로,
사진이 곧 "실제 동작 화면"입니다. **Firebase 불필요.**

> 부저(GPIO)가 연결돼 있으면 기상알람 장면에서 실제로 소리도 납니다.
> 부저가 없어도 화면 촬영은 그대로 됩니다(부저만 생략).

---

## 준비

```bash
cd ~/nagaja          # demo_bridge.py, timer_ui.html 위치
source ~/nagaja/venv/bin/activate
pip install websockets gpiozero lgpio   # 최초 1회 (부저 안 쓰면 websockets만 있어도 됨)
```

## 실행 — 터미널 2개

**터미널 ①: 데모 브리지**
```bash
python3 demo_bridge.py
```
**터미널 ②: 화면(키오스크)**
```bash
chromium --kiosk timer_ui.html
# SSH로 띄울 때: DISPLAY=:0 chromium --kiosk timer_ui.html
```
화면이 브리지에 자동 연결되어 현재 장면을 표시합니다.

---

## 촬영 방법 (키보드로 장면 전환)

터미널 ①에서 **숫자/문자 입력 후 Enter** 로 장면을 바꿔 가며 촬영합니다.

| 키 | 장면 | 촬영 포인트 |
|---|---|---|
| **1** | 여유 (GREEN) | 포인터 초록 구간, 중앙에 다음 수업 시각, 기상알람 카드 |
| **2** | 나가자 (YELLOW) | 노란 카운트다운, "나가자!", 혼잡 +5분 카드 |
| **3** | 지각위기 (RED) | 빨강 카운트다운(깜빡임), "지금 바로 출발하세요" |
| **4** | 비오는날 + 혼잡 | 🌤️비 +10분, 🚦혼잡 +7분 보정 카드 |
| **5** | 폴백 | 하단 "기본값 사용 (AI 예측 실패)" |
| **6** | 기상알람 | 기상 시각 도달 + **부저 울림** (알람 동작 촬영) |
| **a** | 부저 울리기 | 아무 장면에서나 알람 소리만 내기 |
| **s** | 부저 정지 | 알람/부저 끄기 |
| **l** | 메뉴 다시 보기 | |
| **q** | 종료 | |

- 각 장면의 시각은 **촬영 시점의 현재 시각 기준**으로 계산되어, 타이머 링 포인터가
  항상 의도한 구간(여유/나가자/지각)에 정확히 놓입니다.
- **나가자/지각위기**의 카운트다운은 화면에서 실시간으로 줄어들므로, 원하는 숫자일 때 찍으세요.
- 빨강(지각) 카운트다운은 **깜빡임**이 있으니, 글자가 보이는 순간에 촬영하면 됩니다.

### 자동 순환(혼자 촬영할 때)
```bash
python3 demo_bridge.py --cycle 12   # 12초마다 1→2→…→6 자동 전환
```

### 한 장면만 고정
```bash
python3 demo_bridge.py --scene hurry
# relaxed | hurry | late | rainy | fallback | wake
```

---

## 부저 옵션

| 부저 종류 | 실행 |
|---|---|
| 능동형(기본) | `python3 demo_bridge.py` |
| 수동형(XCMT12D2001AP 등) | `python3 demo_bridge.py --passive` |

> 부저 배선/구동은 `HARDWARE_TEST.md` 참고. 부저가 GPIO 직결로 안 울리면(마그네틱 등)
> 화면 촬영만 진행하고 알람은 표시등/문구로 시연하면 됩니다.

---

## 촬영 팁 (카메라)

- **화면 밝기 최대**, 주변 조명은 화면 반사가 적은 위치로.
- 정면에서 살짝 위/아래 각도로 찍어 **반사·무아레(줄무늬)** 를 줄이세요.
- 카메라 노출을 화면에 맞추고, 가능하면 **카메라를 고정**(흔들림 방지).
- 7인치 화면은 800×480이라 가까이서 찍으면 픽셀이 보일 수 있어, 적당히 거리를 두세요.

---

## 문제 해결

| 증상 | 점검 |
|---|---|
| 화면이 "수업 정보 없음" | 브리지 미실행 또는 8765 포트 충돌(`nagaja_bridge.py`와 동시 실행 금지) |
| 연결 안 됨 | 같은 기기에서 `ws://localhost:8765` 인지, 브리지 먼저 실행했는지 |
| 키 입력이 안 먹음 | 터미널 ①에 포커스를 두고 입력+Enter (키오스크 창이 아니라 터미널) |
| 부저 무음 | `HARDWARE_TEST.md` 부저 배선/트랜지스터 참고 (화면 촬영엔 영향 없음) |
